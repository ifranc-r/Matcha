extends layout

block content
  h1= title
  form(action="/register", method="POST")
    input(name="login", type="text", placeholder="Your login")
    input(name="passwd",id="passwd", type="password", placeholder="Enter your password")
    button(type="Submit") Log in
